const fruits = ["Banana", "Laranja", "Limão", "Mação", "Manga"];
const numbers = [10, 6, 2, 85, 23, 1, 3];

const fruitsRevert = fruits.reverse();
const numberRevert = numbers.reverse();

console.log({ fruits, fruitsRevert }, { numbers, numberRevert });
